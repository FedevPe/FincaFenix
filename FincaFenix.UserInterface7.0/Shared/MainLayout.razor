@using FincaFenix.Entities.POCOEntities
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Mvc.Rendering
@inherits LayoutComponentBase

<PageTitle>FincaFenix.UserInterface7.0</PageTitle>

<MudThemeProvider Theme="CustomTheme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<PageTitle>Ordenes de trabajo</PageTitle>

<MudLayout>
    <MudAppBar Elevation="1" Dense="true">
        <MudIconButton Icon="fa-solid fa-bars" 
            Color="Color.Tertiary" 
            Edge="Edge.Start" 
            OnClick="@((e) => DrawerToggle())" />
        <MudText Typo="Typo.h6" Class="ml-2">@TextAppBar.PageTitle</MudText>
        <MudSpacer/>
    </MudAppBar>
    <MudDrawer MiniWidth="70px" Color="Color.Secondary" @bind-Open="_drawerOpen" Elevation="3"
               ClipMode="_clipMode" Variant="@DrawerVariant.Responsive">
               <NavMenu/>
    </MudDrawer>
    <MudMainContent>
        <div class="p-5">
            @Body
        </div>
    </MudMainContent>
</MudLayout>
@((RenderFragment)(builder => base.BuildRenderTree(builder)))
@code{
    [Inject] public UserManager<ApplicationUser> UserManager { get; set; }
    private DrawerClipMode _clipMode = DrawerClipMode.Never;
    bool _drawerOpen = false;
    [Inject] public TextAppBarStateService TextAppBar { get; set; }

    protected override void OnInitialized()
    {
        TextAppBar.ConfigureDispatcher(InvokeAsync);
        TextAppBar.OnChange += StateHasChanged;
    }
    public void Dispose()
    {
        TextAppBar.OnChange -= StateHasChanged;
    }
    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
    MudTheme CustomTheme = new MudTheme()
    {
        PaletteLight = new PaletteLight()
        {
            Primary = Colors.Teal.Darken4,
            AppbarBackground = Colors.Teal.Darken4,
            Secondary = Colors.Teal.Darken3,
            Tertiary = Colors.Gray.Lighten5,
            Info = Colors.BlueGray.Default,
            Error = Colors.Red.Lighten1,
            Warning = Colors.Amber.Darken1,
            Success = Colors.Green.Darken1,
            Surface = Colors.Shades.White,
        }
    };
}
